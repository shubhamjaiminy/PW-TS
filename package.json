{
  "name": "pw-ts",
  "version": "1.0.0",
  "description": "playwright with type script",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "install:deps": "npx playwright install --with-deps"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.0.3"
  }
}
